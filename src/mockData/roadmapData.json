{
  "roadmapTitle": "Understanding the Core Application Flow",
  "steps": [
    {
      "stepNumber": 1,
      "title": "Authentication Module",
      "targetFile": "auth.js",
      "description": "Start here to understand the authentication logic. This module exports the loginUser function that validates user credentials against hardcoded values."
    },
    {
      "stepNumber": 2,
      "title": "The Entry Point",
      "targetFile": "app.js",
      "description": "Read this next to see how the application initializes. The startApp function imports and uses the loginUser function from auth.js to authenticate users and control access to the dashboard."
    }
  ]
}